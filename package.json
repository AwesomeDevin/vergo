{
  "name": "vitedoc-plugins",
  "scripts": {
    "build": "pnpm -r  --parallel build",
    "pub": "pnpm -r  --parallel pub"
  },
  "devDependencies": {
    "@types/node": "^20.11.6",
    "unbuild": "^2.0.0"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}